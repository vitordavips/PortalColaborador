import { Container } from "./styles";

// eslint-disable-next-line react/prop-types
export function Section( {title} ) {
    return (
        <Container>
            {title}
            
        </Container>
    )
}